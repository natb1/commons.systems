#!/usr/bin/env bash
ISSUE_NUM=$(git rev-parse --abbrev-ref HEAD | grep -oE '^[0-9]+')
for sub in $(gh api "/repos/{owner}/{repo}/issues/$ISSUE_NUM/sub_issues" --jq '.[].number' 2>/dev/null); do
  gh issue view "$sub" --json title,body,comments,number,state
done
