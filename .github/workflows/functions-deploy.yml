name: Functions — Deploy

on:
  push:
    branches: [main]
    paths:
      - "functions/**"
      - ".github/workflows/functions-deploy.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy-functions:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version-file: .node-version
          cache: 'npm'
      - name: Install dependencies
        run: .claude/skills/dispatch-propagate/scripts/npm-ci-with-retry.sh
      - name: Build functions
        run: npm run -w functions build
      - name: Authenticate with Firebase
        env:
          FIREBASE_SERVICE_ACCOUNT_JSON: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
          FIREBASE_PROJECT_ID: ${{ secrets.FIREBASE_PROJECT_ID }}
        run: .github/scripts/firebase-auth.sh
      # syncAgenda's non-secret defineString params come from repository
      # Variables, written into the dotenv firebase-tools reads at deploy time.
      # Unset Variables yield empty values, which deploy the function dormant.
      # The private key is the only secret and lives in Secret Manager, not here.
      - name: Write agenda function config
        env:
          AGENDA_GITHUB_APP_ID: ${{ vars.AGENDA_GITHUB_APP_ID }}
          AGENDA_GITHUB_APP_INSTALLATION_ID: ${{ vars.AGENDA_GITHUB_APP_INSTALLATION_ID }}
          AGENDA_GROUP_REPO: ${{ vars.AGENDA_GROUP_REPO }}
          AGENDA_MEMBER_EMAILS: ${{ vars.AGENDA_MEMBER_EMAILS }}
          AGENDA_FIRESTORE_NAMESPACE: ${{ vars.AGENDA_FIRESTORE_NAMESPACE || 'agenda/prod' }}
        run: |
          cat > functions/.env.commons-systems <<EOF
          AGENDA_GITHUB_APP_ID=${AGENDA_GITHUB_APP_ID}
          AGENDA_GITHUB_APP_INSTALLATION_ID=${AGENDA_GITHUB_APP_INSTALLATION_ID}
          AGENDA_GROUP_REPO=${AGENDA_GROUP_REPO}
          AGENDA_MEMBER_EMAILS=${AGENDA_MEMBER_EMAILS}
          AGENDA_FIRESTORE_NAMESPACE=${AGENDA_FIRESTORE_NAMESPACE}
          EOF
      - name: Deploy functions
        run: npx firebase-tools deploy --only functions --force --project "$FIREBASE_PROJECT_ID"
      - name: Cleanup credentials
        if: always()
        run: .github/scripts/firebase-auth-cleanup.sh
