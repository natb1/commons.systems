---
question: What is an expert, and what does the record hold about one between launches?
stage: periagogic
facts:
  - name: answer
    options:
      - name: an-expert-is-an-identity-with-a-scope-and-a-grounding
        source: author
        ref: "2026-09-08"
        supports:
          - words/2026-09-08/36
    recommends: an-expert-is-an-identity-with-a-scope-and-a-grounding
    boldness: low
  - name: authority
    options:
      - name: ratified
      - name: delegated
      - name: deferred
    recommends: ratified
    boldness: low
form: rule
under:
  - commons.systems/disposition-graph/movements
depends:
  - commons.systems/disposition-graph/expert-instructions
  - commons.systems/disposition-graph/delegation
  - commons.systems/disposition-graph/dialogue
  - commons.systems/disposition-graph/readings
---

## Facts

### answer

The recommendation is `an-expert-is-an-identity-with-a-scope-and-a-grounding`, at low
boldness. Low because the option is the author's own words of 2026-09-08 at
`words/2026-09-08/36` and the AI adds nothing to them but the placement and the
observation that the object they name has no definition in the record; the content the
option would carry is owed, which is why this node stands at the periagogic stage with
the option recorded and its content not.

The question exists because the record names an expert everywhere and defines one
nowhere. `delegation` defines the only convened party the record has: "a subagent is a
session the main thread launches for one unit or one lookup, given its brief and
nothing else". That is a one-shot with no name, no persistence, and no address. The
expert of `words/2026-09-08/36` is a different object in three ways at once. It is
"new or existing", so something about it survives between launches. Its identity,
scope and grounding are "recorded in the ref", so what survives is on the record and
not in a session. And a probe may be "marked with target author or (other) expert",
so it can be addressed, which a thing without an identity cannot be. Nothing in the
record holds that object, and every clause of steps 3 through 7 of the entry that says
"expert" refers to it: per-expert accumulation on an option, selection of an option by
an expert, an expert-targeted probe, an expert's choice in its return, and the
grounding between author and experts that gates reconciliation. None of them can be
encoded before the party they attach to is defined.

It is not `expert-instructions`' question. That node asks what an expert convened in
the alignment dialogue is given and where that text lives; it takes the expert as
given and fixes the text handed to it. This node asks what the thing is that the text
is handed to, and what of it the record keeps when no sitting is running.
`movements`' open probe `is-a-convened-position-a-reading-or-a-role` circles the same
gap from the other side without closing it: if a convened position turns out to be a
reading, the identity may already have a home under `readings`, and if it is a role,
it does not.

Two things the author's words leave open, named here so the design does not close them
by silence. Whether the scope is one fact or a set: "Expert scope of fact (most
experts evaluating answer fact)" implies that some experts evaluate `authority`,
`topology` or `persistence`, which decides whether the identity is recorded per fact
or per node. And where on the node the identity is written, which is `dialogue`'s
question about keys and not this node's about the object.

#### an-expert-is-an-identity-with-a-scope-and-a-grounding

An expert is a named party to the alignment dialogue, new or already existing, whose
identity is recorded on the disposition ref beside the disposition it evaluates,
together with its scope -- the fact it evaluates -- and its grounding, some
combination of tradition, recorded graph consistency, and AI judgement. The main
thread's alignment skill selects which experts a movement convenes. Because the
identity is on the record rather than in a session, an expert persists between
launches and can be the addressee of a probe.

**AI support.** The record wrote no case for this option; its support is owed.

**AI divergence.** The record wrote no case against this option; its divergence is
owed.

**Content.**

```markdown
---
question: What is an expert, and what does the record hold about one between launches?
form: rule
under:
  - commons.systems/disposition-graph/movements
---

## Answer

An expert is a named party to the alignment dialogue, new or already existing, whose
identity, scope and grounding are recorded on the ref beside the disposition it
evaluates. Its scope is the fact it evaluates; its grounding is some combination of
tradition, recorded graph consistency, and AI judgement. The main thread selects which
experts a movement convenes. An expert persists between launches, because what
identifies it is on the record and not in a session, and it can therefore be the
addressee of a probe.
```

### authority

The recommendation is `ratified`, at low boldness.

The reading `class-recommendation` calls for: the capture-shaped limb, and squarely.
The experts exist to test the AI's recommendation, so an answer that defines what an
expert is, is the checked party specifying its own checker. Every property this node
fixes is a property of the check: whether an expert persists (a fresh party each time
is a different instrument from a standing one), what its grounding may be (an expert
grounded in AI judgement alone is the AI reviewing itself under another name), and
what its scope covers (an expert scoped away from a fact does not check it). The
author cannot audit the strength of a check from its output, because a weak check and
a passed check produce the same report, which is the shape the limb names. The
irreversible limb is touched and is not the ground: a definition can be redrawn, and
what a wrong one costs is the sittings run under it.

## Account

### Queued, 2026-09-08

Minted in the alignment sitting of 2026-09-08 under the grant at `words/2026-09-08/2`
as refined at `words/2026-09-08/22`, from the author's words at `words/2026-09-08/36`,
which were given while the sitting was in hand and are queued as `movements` and the
alignment skill's queue rule require. The entry states the shape of alignment dialogue
orchestration in seven steps and answers more than one question, so the sitting's first
unit was the decomposition the queue rule calls for. That unit found fifteen questions
in the entry, of which twelve are already asked by an existing node, one is an option
on `probe-or-node`, and two are asked by nothing in the record. This is one of the two.

Placed under `movements` because that node owns the paradigm in which experts are
convened, and because the nearest thing the record had to this question is a probe on
it. It is not placed under `delegation`, whose subagent this object contradicts rather
than refines: `delegation`'s definition is a bound that
`delegation-bounds-and-sizing` puts in the ratified class, and a node that redefined it
from beneath would be widening an authority that only narrows on the way down. What
this node's answer owes `delegation` is a stated relation, not a placement.

Its `depends` names four nodes. `expert-instructions`, because what an expert is given
and what an expert is are one design seen twice, and because that node's recommended
option is the text this one's subject would be handed. `delegation`, because its
one-shot subagent is the object this one departs from, and the departure has to be
recorded rather than assumed. `dialogue`, because an identity recorded on the ref is a
key in a node file and that node owns which keys exist and where each is stored.
`readings`, because `movements`' probe `is-a-convened-position-a-reading-or-a-role`
leaves open whether a convened position is already a reading, in which case the
identity has a home the record has built.

The periagogic object is `delegation` and its definition of a subagent;
`delegation-bounds-and-sizing` and its classification of that definition as a bound;
`expert-instructions`, minted the same day, and its account of what an expert is given;
`movements` and its five open probes, of which three bear on this node; `readings` and
what it holds a reading to be; and `dialogue` on where a part of the dialogue's state
is stored.

### What this node does to `who-convenes-the-experts`, 2026-09-08

`movements` carries the open probe `who-convenes-the-experts`, which asks two things:
who chooses the experts and writes their briefs, and "may that be the AI whose
recommendation they exist to test?". Step 4 of `words/2026-09-08/36` answers the first
-- the main thread's `/align` skill -- and does not touch the second, which is the
capture limb the probe was raised for. The probe is therefore half discharged and must
not be closed: closing it would close both halves, and the half that remains is the one
that matters. It is recorded here rather than on `movements` alone because this node's
`authority` fact rests on the same capture reading, and a later sitting that answers
one should see the other.
