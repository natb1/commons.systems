---
question: What guards a hand-written projection against drift before its generator exists?
stage: periagogic
under:
  - commons.systems/disposition-graph/materialization
---
## Account

Raised by the clean-context reading of
`commons.systems/disposition-graph/review-skills` on 2026-09-05, under
validation 15, the merge: that node's sixth paragraph answers the author's
question about drift in terms that hold of every hand-written projection in
the record and not of the two review skills alone, so the general rule is
recorded on one node's dialogue and reachable only from there. The reading
checked the index for a node that already asks it and found none:
`commons.systems/disposition-graph/transience` asks how transient disposition
is recorded and `commons.systems/disposition-graph/materialization` what
materialized implementation is and where it lives, and neither says what holds
a hand-written projection to its node between the shim's declaration and its
liquidation.

The question is not hypothetical and its instances are the record's own. Every
shim standing today is a hand-written projection in exactly that interval:
`.claude/skills/align/SKILL.md` and `.claude/skills/reconcile/SKILL.md`,
`.claude/skills/align-review/SKILL.md` and `.claude/skills/align-survey/SKILL.md`,
the brief templates and fragments under `packages/clean-context-review/`,
`CLAUDE.md`, the two published pages, and the harness settings. What the
record has meanwhile is three partial guards, all named on `review-skills`:
the shim notice that says the node wins, the currency step that reads the
nodes at every invocation, and the survey's validations that read the skills
as artifacts. On 2026-09-05 a reading and not any of the three found that the
two review skills had already diverged from each other on the model rule
within a day, which is the evidence this question starts from.

The first draft of an answer is `review-skills`'s sixth paragraph, which says
that reconciliation resolves drift by construction at liquidation and not
before, and that the guards in the interim are a check and never a
construction. What this node would settle beyond it: whether a guard stronger
than a check is available before the generator exists, what a shim must carry
so the drift is detectable, and whether the survey's artifact validations are
the right home for the detection.

The periagogic object: the shims declared across the record, read as a set,
against `commons.systems/disposition-graph/transience`, which decides what a
shim declares, `commons.systems/disposition-graph/materialization`, which
decides what a projection is, and the sixth paragraph of
`commons.systems/disposition-graph/review-skills`.
