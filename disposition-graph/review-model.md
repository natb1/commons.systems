---
question: Which model runs the clean-context review's readings?
stage: review
facts:
  - name: answer
    options:
      - name: fable-for-both-readings
        source: author
        ref: "2026-09-04"
      - name: conditional-by-boldness
        source: ai
        ref: "2026-09-04"
        status: passed
        reason: "the author's words of 2026-09-04 put both readings on fable, and it read the reviewer's strength off boldness, which the drafter sets"
      - name: strongest-on-the-survey
        source: ai
        ref: "2026-09-04"
        status: passed
        reason: "the author's words put both readings on fable, and the detection differential it prices the draft's reading on is unmeasured"
      - name: chosen-for-difference
        source: ai
        ref: "2026-09-04"
        status: passed
        reason: "the author's words put both readings on fable, and the difference four models of one lineage can buy is of capability and not the independence the tradition demonstrates"
      - name: fable-until-the-yield-is-measured
        source: ai
        ref: "2026-09-04"
    recommends: fable-for-both-readings
    boldness: moderate
    against: "The draft's reading re-checks, on the same class of mind, a text the most capable model has just written at full attention, so the flat rule pays the top rate where the marginal detection is lowest, buys correlated blind spots where the review wanted independence, and, by pricing both readings the same, forecloses the measurement that would show it."
  - name: authority
    options:
      - name: ratified
      - name: delegated
      - name: deferred
    recommends: ratified
    boldness: low
    against: "The author's words on the viable-options node delegate the right-sizing of models and effort to the AI's judgment for token efficiency, and a ratified model name takes one kind of unit out of that judgment and binds the author to an interview whenever the harness's most capable model is succeeded."
form: rule
under:
  - commons.systems/disposition-graph/clean-context-review
depends:
  - commons.systems/disposition-graph/clean-context-review#per-draft-and-survey
---
## Disposition

The author, 2026-09-04, in the sitting on the alignment page, after granting
its reconciliation and while its two clean-context reviews were running:

> In parallel, progress these adversarial review dispositions through mieutic then you have bootstrap authority to reconcile them immediately:
> - adversarial review during dialogue - both survey and narrow review - use the fable model.

Earlier the same day, in the sitting on the alignment page, directing the two
reviews that were running as these words were said:

> launch align-review using the fable model

## Facts

### answer

The first option is recommended because it is the author's rule and because,
judged from scratch, it is what a reading is for. The party whose blind spots
a clean-context reading hunts is the main thread, which runs on the most
capable model at full effort and reads its own integrated draft adversarially
before the review, so a reader below the main thread's model finds what the
main thread already saw, and that floor is the ceiling, since the harness
offers nothing above it. The reviewer's strength is then a function of
nothing the reviewed party sets, where the rule it replaces read it off
boldness, which the drafter writes. And the price of a reading is bounded and
known while the yield of a weaker one is unmeasured, which makes the
strongest reader on every reading the allocation to make until the yield is
measured. Three of the four others are passed over, the rule the record
carried, the steelman's per-reading rule with the survey on the strongest
model, and a reviewer chosen for difference from the drafter, each against
the author's words and each on a merit stated on its row. One stays viable
beside the author's rule: the same rule with its own return to a per-reading
rule written in for when the measurement exists, which the author may prefer
to a rule revisited by interview. Moderate boldness: the model for both
readings is the author's words, and what rests on the AI is the effort, high
and fixed by the skill; the reading of fable as the name of the delegation
node's role for it, the most capable model, so that the rule outlives the
name; the home of the rule on this node, with the two nodes that stated it
citing it; and the script's silence on a model it no longer computes.

#### fable-for-both-readings

Both readings of the clean-context review, the review of a draft and the
survey, run on fable, the most capable model the harness offers and the one
the delegation node gives the main thread, at high effort, whenever either
runs during the dialogue, and the model is read from nothing on the node and
chosen by no session. The rule is this node's and is stated once; the
clean-context-review node and the decomposition node cite it for the model.
The author's words of 2026-09-04, quoted above.

#### conditional-by-boldness

The rule the record carried in the recommended texts of the
clean-context-review and decomposition nodes and in the review skill: a
draft's reviewer runs on a model never smaller than the drafter's, on fable
when the recommendation's boldness is not low, the node is global-tier, or a
ruling on it would settle other nodes, and on opus otherwise, the skill
reading that from the node; and the survey's reviewer runs on opus, which no
node stated. Its reasons were that a reader weaker than the writer finds what
the writer already saw and that the most capable model on every simple draft
is the cost the delegation node's rule exists to avoid. Passed over: the
author's words put both readings on fable; the writer whose blind spots the
reading hunts is the main thread and not the design unit, so the floor it
argued from is the main thread's model on every draft; and boldness is set
by the drafter, so the rule let the reviewed party select the strength of its
own reviewer.

#### strongest-on-the-survey

The steelman from the tradition survey: a model chosen per reading with its
direction fixed and no input from the drafter, the survey on the most capable
model, since a synthesis across the whole graph is where a stronger reader
finds what a weaker one cannot, and the review of a draft on the larger
model, since it re-checks a text the most capable model has just written at
full attention and a second pass by the same class of mind fails on
correlated inputs. Passed over: the author's words put both readings on
fable, and the detection differential it prices the draft's reading on is
unmeasured, which is the condition under which the strongest reader on every
reading is the allocation to make.

#### chosen-for-difference

A reviewer chosen for difference from the drafter rather than for rank over
it, so that the draft's reading and the survey do not fail on the same
inputs, from the tradition holding that layered defences work only where
their failure modes are uncorrelated and that independence is never assumed
from separate construction. Passed over: the author's words put both readings
on fable, and the four models the harness offers are one lineage, so the
difference a choice among them buys is of capability and is not the
independence the tradition demonstrates; the clean context already removes
the shared framing, and the brief is where any diversity a reading can have
would be bought.

#### fable-until-the-yield-is-measured

The author's rule with its own return written in: both readings on fable at
high effort until the cost and the yield of each reading, the yield being the
findings that changed a verdict, are measured per reading, and a per-reading
rule sized to that measurement thereafter, the measurement being the
instrument the recommended text declares. Viable and not chosen: it is what
the worth of a test prescribes, a reader sized to what its reading changes,
and it names its successor before the measurement exists, which the author's
words do not; under the recommended
text the same measurement is owed and the rule changes by interview when the
measurement shows something, which is the ordinary way a ratified answer
changes.

### authority

Ratified, at low boldness: the author stated the rule in their own words, it
binds how every reading spends their tokens and their attention on every
sitting, and a wrong answer compounds across sittings, which is the record's
test for escalating toward ratified. The case against it is on the fact: the
author's words on the viable-options node leave the right-sizing of models
and effort to the AI's judgment, and a ratified rule takes one kind of unit
out of that judgment, which is what the author did by naming the model
themselves.

## Recommendation

```markdown
---
question: Which model runs the clean-context review's readings?
form: rule
under:
  - commons.systems/disposition-graph/clean-context-review
instrument:
  kind: assessment
  ref: the cost and the yield of each reading, measured per reading, the yield being the findings that changed a verdict, judged by the author against this rule
  note: not yet materialized; no telemetry records a reading's cost, the yield is unmeasured, and the evidence to date is the briefs' sizes on disk and the session's report, in the rationale
---
## Answer

On fable, both of them. The review of a draft and the survey each run on fable, the most capable model the harness offers and the one the delegation node gives the main thread, at high effort, whenever either runs during the dialogue, invoked by a sitting or directly; the model is read from nothing on the node, the draft, or the drafter, and is chosen by no session. The effort is stated in each reading's brief, as the delegation node requires, and fixed by the skill. The rule is stated here and nowhere else: the clean-context-review node says how each reading is run and cites this node for its model; the decomposition node names the review of a draft among a sitting's units and cites this node the same way; the delegation node's rule that the model follows the kind of work is the genus of this one, which names the kind, a clean-context reading of what the main thread wrote or integrated, and fixes its model at the main thread's. The rule is the role's, so that a change of the harness's most capable model moves the readings with the main thread; fable is the author's word for it on 2026-09-04 and the harness's name for it, and the name is amended when the harness's changes, by interview as any ratified text is.

This answer is materialized by the review skill on the implementation ref, the shim the clean-context-review node declares, and by the alignment skill that invokes it, the shim the growth node declares. The review skill's launch step names the model and the effort for either reading, fable at high effort, and its section on model and delegation states this rule and cites this node, in place of the rule that sized a draft's reviewer to boldness, tier, and settling and put the survey on opus. `brief.mjs` computes no model and prints none: `reviewerModel`, the `model` field of both briefs' results, and the tests that pin them are deleted, since a script that prints a constant it does not compute is the projection of nothing. Each brief template states, in its opening constraints, the model and the effort the reading runs on. The alignment skill's statements of the replaced rule, in its list of a sitting's units, its review step, and its section on model and delegation, say instead that both readings run on fable at high effort and cite this node. In the graph, the sentence of the clean-context-review node's recommended text that sizes the reviewer's model, with the rationale sentence and the rejected fixed model that argued it, and the clause of the decomposition node's recommended text that restates it, with the rationale clause and the rejected fixed model there, are replaced by citations of this node.

What this costs, as a consequence and never a reason: every reading pays the top rate. Over the rule it replaces, the difference is the survey's reading and the review of a draft whose recommendation is at low boldness on a node that is not global-tier and settles nothing, each now on fable; every other draft's reading already ran there. What would lower the price of a reading is the brief it is handed, whose index carries every standing answer whole, and never the reader.

## Rationale

The author, 2026-09-04, in the sitting on the alignment page: "adversarial review during dialogue - both survey and narrow review - use the fable model"; and earlier that day, directing the two reviews then running: "launch align-review using the fable model". Judged as if written from scratch, the rule is what the reading is for. A clean-context reading hunts what the party that wrote the text stopped seeing, and that party is the main thread: it runs on the most capable model at full effort, integrates what the design unit drafted, and reads the result adversarially before the review, so a reader below the main thread's model finds what the main thread already saw, and the floor is the ceiling, since nothing above it is offered. The reviewer's strength is a function of nothing the reviewed party sets: the rule this one replaces read it off boldness, which the drafter writes, so the drafter selected the strength of its own reviewer, and that separation, kept wherever a second signature is required, is the one argument for the author's rule the record did not carry. And the price of a reading is bounded and measured while the yield of a weaker one is not: under an unmeasured detection differential, a bounded price, and an irreversible step after the reading, the author's ruling, the strongest reader on every reading is the allocation to make, and the measurement that would let a rule size the reader to the reading is the instrument this answer declares and does not yet have. The effort is high and not the main thread's full effort because the model is the lever for what a reader can hold at once and the effort the lever for depth at each step, and a reading is a checklist run over a neighbourhood or the whole graph, bound by breadth; the effort is fixed with the model so that the session which launches the reading chooses neither. The rule lives here because the author ruled on the model separately from how the review is run, and a decision the author rules on separately is a question and a node, as the dialogue node says; it is stated once, and the two nodes that stated it cite it, as the frontier-consistency node already refuses to restate the review's model. Fable is bound to the delegation node's word for it, the most capable model, so that the rule is in the record's vocabulary and the harness's name for the model is the day's. Measured on 2026-09-04, at implementation commit ed725297 on the greenfield ref: the briefs the two fable readings of that day were handed are 535,197 bytes over 4,651 lines and 428,790 bytes over 3,181 lines; the survey's brief is 519,400 bytes over 4,034 lines with a pins sidecar of 9,676 bytes; the batch brief of 2026-09-03 was 838,923 bytes; and the session that ran the two draft readings reported each at about 480,000 tokens in about fourteen minutes, a figure the repository does not verify, which if right puts a reading at three to five times the brief it was handed.
```

## Account

An un-aligned disposition, recorded from the author's words the turn they
were said. It is a question of its own because the model a reading runs on is
a decision the author rules on separately from how the review is run, which
is `clean-context-review`'s question, and from how work is divided, which is
`delegation`'s.

What the sitting would amend: the rule carried in `clean-context-review`'s
recommended text and in `decomposition`'s, that a draft's reviewer runs on a
model never smaller than the drafter's, on fable when the recommendation's
boldness is not low, the node is global-tier, or a ruling on it would settle
other nodes, and on opus otherwise, and that the survey's reviewer runs on
opus; `delegation`, whose answer has the model follow the kind of work and
names the larger model for judgment; and the review skill's section on model
and delegation with `brief.mjs`, which computes a draft's reviewer from the
node. The periagogic object is those texts, the author's earlier words on
`decomposition` that the more complex recommendations pass to a fable
subagent to review, and the cost evidence this session holds: the two draft
readings of 2026-09-04 on fable, one over a brief of 4,652 lines and one over
3,182, each ran to about 480,000 tokens in about fourteen minutes.

The grant. In the words above the author granted bootstrap authority to
reconcile this disposition immediately after its maieutic movement, before
the clean-context review, which is owed on what is drafted and runs after.

### State at compaction, 2026-09-04

The maieutic movement was opened and divided into units: the tradition
survey is done, at the job's `tmp/review-sitting/traditions.md`, and the
record-and-implementation survey was running, to land at
`tmp/review-sitting/survey.md`; the design unit for this node is briefed at
`tmp/briefs/unit-design-review-model.md` and not launched, and it runs on both
surveys, drafting the model rule for both readings with its options, their status, the recommendation
and its case against, and a fence. Then the main thread's adversarial reading,
the recommendation recorded here with `stage: review`, the reconciliation the
author granted immediately after maieutic, and the clean-context review.

Two findings of the tradition survey are carried here so the design reads
them. The record's rule that a draft's reviewer is never smaller than the
drafter's cites no tradition, and it reads the reviewer's strength off
boldness, which the drafter sets, so the drafter selects the strength of its
own reviewer, a separation-of-duties failure; that is the strongest argument
for the author's rule and it is nowhere in the record. And the drift question
answers itself conditionally: reconciliation resolves drift between two
skills only once the projector writes both from the graph; while the skill is
a hand-written shim, a split makes two hand-maintained copies.

### The maieutic movement, 2026-09-04

Run as the compaction note above says, at graph commit b72d9428 and
implementation commit ed725297: the tradition survey and the
record-and-implementation survey were read whole by the design unit, which
drafted the recommendation above on fable, since the draft amends an
ancestor's recommended text and bears on a global-tier node, as
`decomposition`'s rule for the design unit's model requires. The
recommendation is recorded before any periagogic probe was answered, under
the author's direction to progress this disposition through the maieutic
movement and reconcile it immediately, so that the two places where the
answer reads the author's words rather than repeating them are visible at
the ruling: fable taken as the name of `delegation`'s role for it, the most
capable model, so that the rule outlives the name; and "during dialogue"
taken as the alignment dialogue's review whenever it runs, the survey
invoked directly included, since its object is the frontier the dialogue is
about, and not as a bound that would leave a direct invocation to
`delegation`'s tiers.

**The three classes of finding.**

Within the graph. The author's words contradict two recommended texts:
`clean-context-review`'s fence puts a draft's reviewer on fable only when the
recommendation's boldness is not low, the node is global-tier, or a ruling on
it would settle other nodes, and on opus otherwise, and `decomposition`'s
fence restates that rule in near-identical words, with a third partial
statement in its option `seams-and-split-review`; the survey's model is in no
node, and the skill alone puts it on opus, cited to `delegation`'s "larger
model for design and judgment, such as a layout or a survey that classifies
what it reads", which names no model. `frontier-consistency`'s fence refuses
to restate the review's model and names `clean-context-review` as its home,
so by the record's own validations 9 and 12 `decomposition`'s copy is already
the duplicate; this answer makes this node the home, since the author ruled
on the model separately, and has both cite it. `growth` keeps "fable as the
default model" from the incumbent skill as a principle while `delegation`
rejects "a fixed model for every task"; both stand, since this rule is a
fixed model for one kind of work, which `delegation`'s rule permits and of
which `growth`'s principle is the general case, and neither is amended. `delegation` speaks
in role-words, the smallest, the smaller, the larger, the most capable, and
no node binds one to a model's name; the binding lives in the two skills.
This answer binds one, fable to the most capable model, the first binding
the graph carries, which is why it is stated as the role's rule with the
name as the day's.

Between the graph and the AI's knowledge. The rule the record carried argued
from a floor, that a reader weaker than the writer finds what the writer
already saw, and took the writer to be the design unit, whose model varies;
but the text a reading receives has passed through the main thread, which
runs on the most capable model at full effort and reads the integrated draft
adversarially before the review, in the order `decomposition`'s fence gives
the units, so the floor's own reasoning puts every reading on the main
thread's model, which is the author's rule. The Agent tool offers exactly
four models, sonnet, opus, haiku, and fable, so every role-word `delegation`
uses has a target and fable is selectable per subagent; a fork ignores a
model override, which is consistent with a reading never being a fork. The
tool takes no effort parameter: a subagent's effort comes from its type's
definition, and "effort high" in the skill's launch step is an instruction
the reviewer reads, which is where `delegation` says the effort goes, in the
brief; verified that neither brief template states an effort or a model
today, which the recommended text has them do. `brief.mjs` computes a draft's
model in `reviewerModel`, called once, and writes the survey's as the
literal `"opus"`; the CLI prints both, `brief.test.mjs` pins both, and
nothing enforces the printed value, since the session launches the reviewer
by hand; under the flat rule the function is dead and the printed line a
constant. The compaction note's "about 480,000 tokens in about fourteen
minutes" per fable reading is the session's report and not verifiable from
the repository, which records no session telemetry; what is verifiable is
measured below.

Redundant seams. The rule is stated in five prose sites of the
implementation and one function: `.claude/skills/align-review/SKILL.md` §2,
the line saying the script "prints the reviewer's model it computed from
the node, `fable` or `opus`, by the rule in §3"; §3, the launch step and the
sentence beginning "A draft's reviewer runs on a model never smaller than
the drafter's"; and "Model and delegation"; `.claude/skills/align/SKILL.md`
§1, the unit list's "whose model `/align-review` reads from the node"; §5,
the sentence beginning "The reviewer's model is read from the node by the
review skill and never argued in a brief"; and its "Model and delegation";
and `reviewerModel`'s doc comment. One change touches all of them, and the
recommended text names each. The instrument this answer declares, the cost
and the yield of each reading measured per reading, and the telemetry the
`review-skills` question is about are one measurement seen from two nodes;
recorded so that a ruling on either does not read the other as settled, and
so that the split, if the author rules for it, is known to be what would
make this rule re-decidable on evidence.

**Evaluated twice.**

Fresh judgment produced the answer above by three arguments that each stand
alone: the party under review is the main thread, so the floor for a reading
is its model and the floor is the ceiling; the reviewer's strength is read
from nothing the reviewed party sets; and under an unmeasured yield, a
bounded price, and an irreversible step after the reading, the strongest
reader on every reading is the allocation to make. The effort followed from
the model being the lever for breadth and the effort the lever for depth, a
reading being bound by breadth.

The pass with reference to tradition read the tradition survey's readings on
this question. Six informed the resolution and are owed as reading nodes
under this one, named here for the main thread to mint with the relation
each bears: segregation of duties, from internal control and the two-person
concept, adopted on `fable-for-both-readings`, for the reviewer's strength
read from nothing the drafter sets, and diverged from on
`conditional-by-boldness`, which read it off boldness; scholarly peer
review, from the Royal Society's Committee on Papers of 1752 and Baldwin's
history of 2018, adopted on `fable-for-both-readings`, for one standard of
reader across both readings, the peer of the main thread being the main
thread's model; value of information, from Howard's paper of 1966 and Raiffa
and Schlaifer's book of 1961, adopted on `fable-for-both-readings` for the
strongest reader under an unmeasured yield and on
`fable-until-the-yield-is-measured` for the return it writes in, and the
source of the instrument the answer declares; appellate review by a heavier
bench, from the en banc rehearing on a question of exceptional importance,
adopted on `conditional-by-boldness`, which is that rule in its purest form,
and diverged from on `fable-for-both-readings`, since it grades the bench by
the question's importance and every ruling here is the irreversible step;
the surgical team, from Brooks, diverged from in part on
`fable-for-both-readings`, since the most capable model makes the draft here
and checks it too, so the allocation it prescribes is kept rather than
inverted; and N-version programming with the independence experiment, from
Avizienis and Chen and from Knight and Leveson, adopted on
`chosen-for-difference` and diverged from on `fable-for-both-readings`,
since the difference four models of one lineage can buy is of capability and
is not the independence the experiment demanded be demonstrated. Surfaced and
not minted, since they informed no resolution here: the promotor fidei, a
role argument that bears on the named office `review-skills` asks about;
Weinberg's egoless programming, whose mechanism is human ego; and pair
review at the point of writing, already owed on `decomposition` and bearing
on timing rather than on model. The tradition survey's finding before its
readings is carried into the rationale: the rule the record carried cited no
tradition, and the review traditions grade the reviewer by independence, by
peerhood, or by role, never by rank over the author, the one exception
grading by the question's importance.

The steelman from the traditions is the per-reading rule with its direction
fixed, the survey on the strongest reader and the draft's on the larger
model chosen for difference from its drafter, with boldness removed as an
input. It is recorded as two options, passed over on their rows, and it is
answered on the merits twice: the text the draft's reading checks is the
main thread's and not the design unit's, so the re-check it calls low-yield
is a second reading of the most capable model's own integrated text, which
is the case the separation exists for; and the differential it prices the
draft's reading on is the unmeasured quantity, so the rule it proposes is
the one the measurement would have to license. Its strongest point stands
and is the case against on the recommended row: one price for both readings
measures nothing, and the flat rule forecloses the evidence that would settle
this question, until the readings are measured per reading.

**Tested against the record it joins.**

The `under` chain: `clean-context-review`, `recording`, `growth`, `model`,
`purpose`, and `commons.systems/public/agency` at the root, every node of it
unanswered, so nothing in it grants authority and the author's grant quoted
above is what licenses the reconciliation. What the chain fixes and this
answer keeps whole: two readings divided by their object, each a fresh
context that is never a fork, a reviewer that writes nothing, and a session
that validates every finding on its own thread. The global-tier nodes:
`materialization`, under which the skills' model sentences are projections
of this node, carried on the two declared shims and liquidated with them,
and a function that computes nothing is unsupported implementation, pruned
here; `delegation`, of whose rule this one is a case, with the effort stated
in the brief as it requires; `evaluation`, under whose lens no argument from
what is built appears in the answer, the cost is stated as a consequence,
and the evaluation ran twice; `authority`, under which nothing on this node
acts, the class is the author's to confer, and what would contradict another
recommended text is recorded rather than applied; and `session-context`,
untouched, since a skill is none of the three things a session loads. The
answer contradicts no doctrine, because nothing is ratified. It contradicts
one sentence of `clean-context-review`'s recommended text and one clause of
`decomposition`'s, both marked the AI's on those nodes' answer facts and
both within the author's grant to reconcile this disposition immediately;
the recommended text names them, and amending them under the grant moves
each node's recommendation at the review stage, where both already stand
with their review owed. The alternative, an option on each node's answer
fact carrying a whole fence for a one-sentence citation, is not taken, since
neither node's question is the review's model. `review-skills` is the
sibling this answer is read beside: the rule is the same whether the two
readings are one skill or two, and the sentence the skill carries is carried
by each if they divide, so no dependency is recorded; the measurement this
answer owes is what that split would name.

**The map of decisions to fields.** The model for both readings is the
answer fact's `fable-for-both-readings`, recommended at moderate boldness
with the case against on its row; the rule the record carried is
`conditional-by-boldness`, passed over with the author's words and the
separation as its reason; the steelman's per-reading rule is
`strongest-on-the-survey` and the diversity rule `chosen-for-difference`,
each passed over with its reason; the bounded flat rule is
`fable-until-the-yield-is-measured`, viable and not chosen. The effort, the
reading of fable as the role's name, the home of the rule, and the script's
silence are the recommended text's and are what the boldness is about; the
effort is not an option of its own, since the question is the model and the
effort is the brief's under `delegation`, and full effort, the main
thread's, is the edit the author would make to the recommended text if they
want the peer of the main thread at its effort too. The class is the
authority fact, ratified at low boldness with its case against. There is no
persistence fact, since the node's shape does not change, and no existence
fact, since no prune is proposed. `depends` names
`clean-context-review#per-draft-and-survey`, the side of that node's
divergence this rule stands under, since a model for each of two readings
presupposes the two. The instrument is declared in the fence and is
unmaterialized, and its note says so. The six readings are owed as nodes.
The sentences that change are named in the recommended text and located
here, at the commits read: in `clean-context-review`'s fence, the paragraph
"The review of a draft.", its sentence beginning "Its model is never smaller
than the drafter's", the rationale's "The reviewer's model floor follows
from what a review is for: a reader weaker than the writer finds what the
writer already saw", and its rejected "a fixed model for every review, which
pays the most capable model on every simple draft or reviews a bold one in
name only", with the `### answer` clause "and the reviewer's model floor are
the AI's"; in `decomposition`'s fence, the paragraph "The kind of analysis.",
its clause "on a model never smaller than the drafter's and the most capable
one when the recommendation's boldness is not low, the node is global-tier,
or a ruling on it would settle other nodes", the rationale's "and the rule
that a reviewer's model is never smaller than the drafter's, since a review
by a weaker reader than the writer catches what the writer already saw", its
rejected "a reviewer chosen by a fixed model, since the most capable model on
every simple draft is the cost the delegation node's rule exists to avoid,
and a smaller one on a bold draft is a review in name", the clause of
`#### seams-and-split-review` "on a model never smaller than the drafter's
and the most capable one when the recommendation is bold or the node settles
others", and the `### answer` clause "and the rule sizing the reviewer's
model to the drafter's are the AI's"; `.claude/skills/align-review/SKILL.md`
lines 139 to 140, 191 to 200, and 331 to 338;
`.claude/skills/align-review/brief.mjs` lines 150 to 172, 618, 706, 740, and
749; `.claude/skills/align-review/brief.test.mjs` the import at line 17, the
test at lines 194 to 227, and the assertions at lines 234, 241, 248, 275,
and 355; `.claude/skills/align-review/brief-draft.md` and `brief-survey.md`,
the opening constraints paragraph of each; and
`.claude/skills/align/SKILL.md` lines 172 to 173, 620 to 624, and 792 to
797.

**The evidence, measured on disk on 2026-09-04 at implementation commit
ed725297.** The briefs under `tmp/review/`, by `wc -l -c`:

| brief | lines | bytes | reading |
|---|---|---|---|
| `draft-alignment-page.brief.md` | 4,651 | 535,197 | a draft's, on fable |
| `draft-prose-and-structure.brief.md` | 3,181 | 428,790 | a draft's, on fable |
| `draft-decomposition.brief.md` | 2,870 | 420,361 | a draft's |
| `survey.brief.md` | 4,034 | 519,400 | the survey's, with `survey.pins.json` at 9,676 bytes |
| `frontier.brief.md`, 2026-09-03 | 5,776 | 838,923 | the last batch |

The two fable briefs end without a trailing newline, which is why the
compaction note counts 4,652 and 3,182 lines. The session's report of about
480,000 tokens per fable reading is not verifiable here; at four bytes per
token the two briefs are about 134,000 and 107,000 tokens, so the report, if
right, puts a reading at between three and five times the brief it was
handed, and the reading and not the brief is the greater part of the price.

Responses open: confirm the recommended text; confirm with edits; deny with
feedback; or rule for another option on the fact.

This recommendation is not reviewed. The main thread's adversarial reading
comes next, then the reconciliation the author granted immediately after the
maieutic movement, and the clean-context review runs on what is reconciled,
as the grant above says.

### Recorded, 2026-09-04

Recorded at the review stage by the main thread after its adversarial
reading, at graph commit 0a0bcb9c and later. Between b72d9428, which the
design read, and that head, the migration of 2026-09-04 moved the rejected
sentences the map above locates in the two fences into options with the
status passed on their answer facts, `fixed-model-for-every-review` on
clean-context-review and `reviewer-on-a-fixed-model` on decomposition; the
reconciliation treats those options as the map treats the sentences, and
since the author's words revive a fixed model for every reading, each loses
the status passed and its subsection says the question moved here. The
reading found nothing to redraw: the three arguments stand alone, the
options are the frontier the traditions and the record give, and the
boldness is right for what rests on the AI.

At the second compaction of 2026-09-04 the reconciliation unit was briefed at the job's tmp/briefs/unit-split-review-skills.md and not launched, waiting on the unit reconciling the review scripts to the new encodings, since both write the same files; the readings unit for both nodes was running, to tmp/review-sitting/readings/; and the order that remains is on alignment-page's account.
### Reconciled, 2026-09-04

Landed at `a1ddc6e6` on the implementation ref with the split, under the
author's bootstrap grant of 2026-09-04 and before any ruling on this node,
which is unanswered and whose recommendation acts on nothing. `brief.mjs` no
longer computes a reader's model: `reviewerModel`, the `model` field of both
results, both places the command line printed it, and the shapes in the
JSDoc are gone, and one test now pins that no model is returned and none
printed, in place of the test on the old formula. Each skill states the model
and the effort at the launch, in a short **Model and delegation** section that
cites this node and states no rule of its own; `/align`'s three sentences on
the reader's model, in its first and fifth sections and its section on model
and delegation, now say fable at high effort for both readings and cite this
node in place of the formula that sized the reader to the drafter's model and
the recommendation's boldness. Where `review-skills`' recommended text still
said the brief step prints the reader's model, this node's recommendation was
followed, as that node itself defers the model here; the conflict is recorded
on `review-skills` for the survey.
