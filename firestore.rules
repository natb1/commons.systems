rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // BEGIN: landing
    // Messages are publicly readable; notes require authentication (see below)
    match /ns/{namespace}/messages/{messageId} {
      allow read: if true;
      allow write: if false;
    }

    // Notes require authentication
    match /ns/{namespace}/notes/{noteId} {
      allow read: if request.auth != null;
      allow write: if false;
    }

    // Published posts are public; unpublished posts require authentication
    match /ns/{namespace}/posts/{postId} {
      allow read: if resource.data.published == true || request.auth != null;
      allow write: if false;
    }
    // END: landing

    // Deny everything else by default
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
