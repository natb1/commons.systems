import type { AuthUser } from "@commons-systems/authutil/seed";

const helloTestUser: AuthUser = {
  localId: "test-github-user",
  email: "test@example.com",
  displayName: "Test User",
};

export default helloTestUser;
