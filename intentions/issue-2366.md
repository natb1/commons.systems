---
id: issue-2366
statement: "Intention tree: schema, local store, and read-only backfill"
owner: human
status: raw
parent: issue-2100
rationale: >-
  - Define the node schema as one markdown-with-frontmatter file (the
  memory-system
    convention): `id`, `parent`, `statement`, `rationale`, `owner`
    (`human`|`ai`|`procedure`), `status` (`raw`|`refining`|`delegated`|`codified`),
    `clarifications[]` (the dialectic Q&A that refined it), `tooling_goals[]`,
    `success_signal` (`observable`, `sensor`, `threshold`, `is_proxy`), `reading`,
    `gap`.
  - Define the store layout (a repo-local directory, e.g. `intentions/`).

  - Read-only backfill: roots from CHARTER principles, leaves from current open
    issues' scope, linked parent ↔ child.
reading: >-
  - Define the node schema as one markdown-with-frontmatter file (the
  memory-system
    convention): `id`, `parent`, `statement`, `rationale`, `owner`
    (`human`|`ai`|`procedure`), `status` (`raw`|`refining`|`delegated`|`codified`),
    `clarifications[]` (the dialectic Q&A that refined it), `tooling_goals[]`,
    `success_signal` (`observable`, `sensor`, `threshold`, `is_proxy`), `reading`,
    `gap`.
  - Define the store layout (a repo-local directory, e.g. `intentions/`).

  - Read-only backfill: roots from CHARTER principles, leaves from current open
    issues' scope, linked parent ↔ child.
gap: null
clarifications: []
tooling_goals: []
success_signal: null
---
# Intention tree: schema, local store, and read-only backfill
