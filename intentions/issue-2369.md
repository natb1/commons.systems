---
id: issue-2369
statement: "Delegability evaluation: CAN classifier and SHOULD push-down economics"
owner: human
status: raw
parent: issue-2100
rationale: >-
  - **CAN classifier:** (a) systematizable → procedure; (b) needs broad pattern
    recognition → AI; (c) too ambiguous → human, refine. Output may be "human-for-now,
    plus a tooling-goal to make it AI-ownable" — the boundary moves down over time as
    tooling improves.
  - **SHOULD economics:** push-down ROI = build + run + maintenance cost vs.
  manual
    cost × frequency, counting the higher-order attention freed. Consults the
    intrinsic financial and technical perspectives. A net-negative push-down is
    declined even when feasible.
  - A documented input/output contract: input is an intention node; output is an
  owner
    recommendation + rationale + optional tooling-goal, usable by the dialectic
    engine.
reading: >-
  - **CAN classifier:** (a) systematizable → procedure; (b) needs broad pattern
    recognition → AI; (c) too ambiguous → human, refine. Output may be "human-for-now,
    plus a tooling-goal to make it AI-ownable" — the boundary moves down over time as
    tooling improves.
  - **SHOULD economics:** push-down ROI = build + run + maintenance cost vs.
  manual
    cost × frequency, counting the higher-order attention freed. Consults the
    intrinsic financial and technical perspectives. A net-negative push-down is
    declined even when feasible.
  - A documented input/output contract: input is an intention node; output is an
  owner
    recommendation + rationale + optional tooling-goal, usable by the dialectic
    engine.
gap: null
clarifications: []
tooling_goals: []
success_signal: null
---
# Delegability evaluation: CAN classifier and SHOULD push-down economics
