---
id: strategy-diversify-income
kind: strategy
statement: Rebuild the income pipeline diversified — no single client sets the problem
owner: human
status: refining
parent: null
rationale: >-
  Minted directly on delegation-client-income's review trigger per
  strategy-domain-selection's caveat for non-software recoveries (2026-07-02):
  recovery here is pipeline work, not software, so it never scores against the
  software-shaped criteria. The honest reading changes the shape of the work:
  there are currently no active clients, so this is not unwinding a live
  concentration — it is rebuilding a pipeline with diversification as a design
  constraint, so the concentration capture never re-forms. The imported virtue
  the record names is structural — client priorities shape where attention goes
  — and it is imported in proportion to concentration: one dominant client sets
  the problem; several bounded ones merely bid for attention the author still
  allocates.


  While the pipeline is empty, strategy-financial-sustainability's runway rule
  is the binding constraint, and the same owned sensor reads both signals:
  income sources and their shares are visible in the budget pipeline
  (strategy-recover-finance), so no new instrumentation is needed. The
  non-delegable floor stays the record's — the ability to find and close new
  client work is exercised only by doing it, and an empty pipeline left unworked
  is that floor atrophying.
reading: no active clients; the pipeline is empty (owner interview, 2026-07-02)
gap: reading "no active clients; the pipeline is empty (owner interview,
  2026-07-02)" does not meet threshold "at least two independent active income
  sources, and no single client above half of trailing income"
serves:
  - virtue-alignment-of-attachments
recovers:
  - delegation-client-income
clarifications:
  - question: What concrete business-development approach (channels, target
      segments, offer shape, outreach cadence) does the diversified pipeline
      rebuild use?
    answer: Unrecorded as of this round, and decomposition depends on it — proposed
      for author ratification. The signal path (finding and closing new client
      work) is the record's non-delegable human floor, and the record states no
      new instrumentation is needed (the owned budget pipeline already reads
      income sources and shares), so the round's only executable shape is
      born-parked author tactics chunked to <=30 author-minutes. That chunking
      requires the concrete approach, which the strategy does not record; any
      chunking invented here would import unrecorded design assumptions the
      author must ratify. Recorded 2026-07-11 /align-tactics round.
tooling_goals: []
success_signal:
  observable: active independent income sources, and the largest client's share of
    trailing income
  sensor: the owned budgeting pipeline (strategy-recover-finance), read at office-hours
  threshold: at least two independent active income sources, and no single client
    above half of trailing income
  is_proxy: false
attention: null
phase: null
execution: null
validates: []
blocked_by: []
office_hours:
  reason: "align-tactics round (2026-07-11): decomposition blocked on requirement
    ambiguity / record completeness. The strategy records the intent to rebuild
    the pipeline diversified but not the concrete business-development approach
    needed to chunk the non-delegable human work into <=30-author-minute
    born-parked tactics; a single 'do business development' tactic would restate
    the strategy and break the chunking rule. No claude-eligible signal-path
    tactic exists: the rationale names finding and closing client work as the
    non-delegable floor, and explicitly rules out new instrumentation. No
    recorded condition has demonstrably failed (the empty pipeline is the
    rebuild premise, not evidence that consulting demand is absent), so this is
    not a failed-condition park. Clarification 1 (the sole entry) proposes the
    missing approach for ratification — a record-completeness gap of the
    2026-07-02 /align-strategy round."
  since: 2026-07-11
  recommendation: At office-hours, either (a) run /align-strategy to record the
    concrete pipeline-rebuild approach (channels, target segments, offer shape,
    outreach cadence), then re-run /align-tactics to chunk born-parked author
    tactics plus any claude-eligible surfaces the approach identifies (e.g.
    outreach or landing copy behind the author-approved-copy gate); or (b)
    decide the rebuild is inherently ongoing human effort tracked via the
    office-hours budget-pipeline signal read, and leave the strategy parked
    without decomposition.
pace_exempt: false
rounds:
  count: 0
  last_completed: null
attributes:
  conditions:
    - demand exists for the author's consulting at individual scale
    - diversification stays compatible with depth — a pipeline of many shallow
      engagements imports its own attention scatter
---
# Rebuild the income pipeline diversified — no single client sets the problem
