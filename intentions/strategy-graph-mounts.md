---
id: strategy-graph-mounts
kind: strategy
statement: Mounting is first-class — external graphs (delegatees, traditions,
  institutions, persons) graft on recursively as explorable structure with
  auditable degree of capture and deference
owner: human
status: refining
parent: strategy-explicit-intent
rationale: "kind-delegation and kind-tradition already describe mounting in
  prose — every delegation grafts the delegatee's virtues onto this graph as
  constraints; adopting a tradition grafts its vocabulary and problem-framings —
  but the graft's content is write-only prose: divergence.imported and
  adopted/diverged are string lists, so nothing can traverse from
  strategy-financial-sustainability to the commercial growth virtue it partly
  holds, no sensor can read drift in an imported virtue that has no node, and
  delegation-client-income's live graft (client/employer duties) is invisible to
  the dialectic. This strategy makes the mount structure: a mounted graph's
  virtues, strategies, and duties become nodes; a distinct graft relation —
  never serves, which stays native-only — records what motivates what across the
  mount boundary, so native versus grafted motivation is queryable by relation
  type and the goals page can render the boundary honestly. Degree of capture
  and deference is carried twice with distinct roles: the record's hand-assessed
  verdict (today's divergence.level and classification) and a derived measure
  computed from how much motivation flows across the boundary — disagreement
  between the two is itself a drift signal. Mounts are recursive: a family's
  graph mounts its individuals' graphs, the author may mount an individual's
  graph, an institution mounts another institution (a vendor mounts a governing
  body, or another vendor); under the by-model discipline recursion is nested
  modeling to the depth the audit needs. Scope covers every counterparty class —
  vendors, traditions, individuals, families, client/employer relationships,
  governing institutions — and the existing delegation-* and tradition-* records
  anchor their mounts: one system, the prose lists migrate into structure, no
  parallel ledger for strategy-complete-ledger to sweep twice. Timing:
  strategy-attention-surface's goals page (boost 3, in flight) already promises
  delegation-and-capture views; landing mount structure first keeps that page
  from baking in a prose-attribute rendering that would be redone."
reading: null
serves:
  - virtue-alignment-of-attachments
  - virtue-respect-for-persons
recovers: []
clarifications:
  - question: What is the substance of a mounted graph — the author's model of the
      counterparty, or a reference to a graph the counterparty owns?
    answer: By-model now, by-reference-ready shape. A mount is author-authored nodes
      in this repo modeling the counterparty's virtues, strategies, and duties —
      the author's model of the vendor, not the vendor's self-description — but
      the data structure must not assume same-repo residence, so a by-reference
      mount (a household member's or fork practitioner's actual graph store, per
      the align-init consumer path) is the same shape later. Recorded 2026-07-07
      interview.
  - question: Is the graft-motivation edge (strategy-financial-sustainability to a
      mounted commercial virtue-growth) the same serves relation used for native
      virtues?
    answer: No — a distinct relation. serves stays native-only (validateGraph rule 8
      intact), and grafted motivation crosses the mount boundary through its own
      edge type, so 'what do I hold because of a graft' is queryable by relation
      type and the goals page renders native versus grafted motivation
      differently — which is exactly the make-it-clear-I-grafted-this-on
      requirement. Recorded 2026-07-07 interview.
  - question: Person-mounts (family members, client/employer) name real people in a
      public repo — what is the privacy boundary?
    answer: "Role-named plus consent for now: person and institution mounts are
      role-named (never legal names), model duties and virtues only, and where a
      household member is identifiable even by role,
      strategy-household-shared-attachments' consent practice applies before the
      mount lands. Target state inverts the relation: the author will keep a
      private graph of personal intentions, and this public graph becomes a
      mount on that private root, retaining only the intentions needed to
      justify the intention-graph data structure, the dispatch harness, and the
      office-hours project. The migration path to that inversion must be
      documented (retained as tactic-private-root-migration-path). Recorded
      2026-07-07 interview."
  - question: Is degree of capture/deference hand-assessed or derived from structure?
    answer: "Both, with distinct roles. The hand-assessed judgment stays on the
      record — today's divergence.level and classification are the author's
      verdict. The structural degree is derived: how much motivation flows
      across the mount boundary (count and weight of graft edges into native
      strategies), computed by tooling and rendered on the goals page.
      Disagreement between the two is a review signal — a low-divergence record
      with heavy boundary flow is the drift detector. Recorded 2026-07-07
      interview."
  - question: How do the existing delegation-* and tradition-* records relate to mounts?
    answer: "Records anchor mounts. Mounting generalizes what kind-delegation and
      kind-tradition already describe in prose: each existing record becomes or
      carries the mount anchor for its counterparty, and its imported/adopted
      prose lists migrate into mounted subgraph structure over time. One system,
      no parallel ledger; new counterparty classes (persons, families, governing
      institutions) get records of the same family. Recorded 2026-07-07
      interview."
  - question: What does recursive mounting mean concretely?
    answer: "A mounted graph may itself carry mounts: a family's graph mounts the
      graphs of the individuals in it, and the author may mount an individual's
      graph; an institution mounts the graph of another institution — a vendor
      mounts a governing body, or another vendor. Under the by-model discipline
      this is nested modeling authored to the depth the audit needs, not an
      obligation to model any counterparty exhaustively; the same structure must
      hold when a mounted graph is by-reference. Recorded 2026-07-07 framing
      interview."
  - question: How does round 1 satisfy the human-authored-substance condition while
      agent sessions build the mounted structure?
    answer: "By the condition's own drafting allowance: the migration tactics
      transpose only the already-human-authored prose (divergence.imported
      lists, adopted/diverged/chosen_over entries, and the growth-via-spend
      capture recorded on delegation-anthropic-claude) into mounted nodes and
      graft edges, inventing no new claims — where the prose is too thin to
      model a node, the gap is listed for the author instead of filled. The
      author's ratification is the born-parked tactic-mount-owner-review, which
      amends or approves the mounted structure at office-hours and produces the
      strategy's first reading. Informative, not gating. Recorded 2026-07-11
      /align-tactics round."
  - question: What is the capture model — capture as a function of delegation — and
      what does it require of node composition?
    answer: "(Recorded 2026-08-30 interview; author-ratified, Claude-drafted from
      the author's worked example.) DEFINITION: capture(mount) = the weighted
      share of decision-motivation flowing across that mount's boundary, derived
      from two flow classes read off existing structure — AUTHORITY flows
      (decision stamps in strategy-explicit-intent's three-state model naming
      the mount as delegatee; review-declined weighs more than pending-review)
      and MOTIVATION flows (this strategy's graft edges into native nodes) —
      weighted by graph position (root-distance: virtues at the roots produce
      the strongest signal structurally, per the author's parsimony ruling
      rejecting special-case alarms), with the reading's consumer being the
      comparison against the mount's hand-assessed divergence verdict — the
      dual-carry doctrine already recorded here, disagreement being the drift
      signal. NO influenced_by field: the author's example ('Kantian respect and
      Socratic education would compel me to offer this knowledge for free, but
      the technology platform fees compel me to monetize') decomposes as native
      serves motivation (no capture), a graft-edge constraint from the strategy
      to the platform mount (influence flow — still the author's decision), and,
      had the pricing decision been handed over, a delegated stamp (authority
      flow, heavier); the platform reshaping the virtue itself would be a
      virtue-layer flow, loudest. Influence is node-shaped — if any decision on
      a node stands under a constraint, the node does — so the graft edge this
      strategy already promises carries it, and per-decision granularity is
      reserved for authority, where review rights actually differ. DECOMPOSITION
      COROLLARY (author-ratified): node-level influence granularity is accurate
      only while nodes are motivation-coherent, so — decompose a node when its
      decisions would carry materially different motivation profiles (a
      different serves set or different graft constraints); compose or merge
      only when profiles match. This is the structural dual of /align's
      multi-topic separation rule (which keys on independent success signals;
      this keys on motivation) and is the load-bearing justification for having
      no per-decision influence field. OPEN GAP (author-directed, for a
      dedicated /align round): 'my need to feed myself' has no first-class
      virtue home — temperance covers nourishment as ordered appetite,
      strategy-financial-sustainability is instrumental ('fund the project') —
      the sustenance/livelihood round's recommendations must defer to
      philosophical, theological and/or psychology tradition. RESOLVED
      2026-08-30 (same-day sustenance round): virtue-right-livelihood ratified
      as the first-class home; virtue-knowledge-as-gift held
      delegated-pending-review as its tension sibling;
      strategy-financial-sustainability re-homed to serve the new root. The
      tradition-deference instruction was followed — substantiating and
      contradicting traditions recorded per strategy-explicit-intent's
      substantiation doctrine."
tooling_goals:
  - kind: actuator
    statement: mount schema — mount anchors on delegation/tradition records,
      mounted-node residence and naming, the distinct graft relation, recursion,
      by-reference readiness, and validate-graph rules over mount structure
  - kind: actuator
    statement: goals-page mount views — render mount boundaries, hand-assessed and
      derived degree, adopted virtues/strategies/duties, and the
      motivating-dependencies query from any strategy
  - kind: sensor
    statement: derived-degree computation — motivation flow across each mount
      boundary, compared against the record's hand-assessed level, with
      disagreement surfaced as a review signal
success_signal:
  observable: from any strategy, the goals page enumerates the mounted
    dependencies that motivate it — mount boundary, grafted
    virtues/strategies/duties, and both degrees (hand-assessed and derived)
    visible
  sensor: owner review at office-hours over the goals page, plus validate-graph
    rules over mount structure
  threshold: the canonical query — strategy-financial-sustainability surfaces the
    grafted commercial growth virtue — answers from graph structure alone, and
    every high-divergence delegation record and every tradition with adopted
    entries carries mounted structure, not only prose lists
  is_proxy: false
attention: null
phase: null
execution: null
validates: []
blocked_by: []
office_hours: null
pace_exempt: false
rounds:
  count: 0
  last_completed: null
  last_aligned: null
attributes:
  conditions:
    - mounted content is the author's model of the counterparty — substance
      defaults to author authorship with agent assistance as drafting, extending
      strategy-explicit-intent's amended authorship condition to mounts;
      delegating it carries a decision stamp and the capture model's steepest
      position price (amended 2026-08-30 with the parent condition, under the
      legacy-null migration rule)
    - mounts are modeled to the depth the audit needs — the graph including
      mounts stays enumerable and maintainable at individual scale, so
      strategy-complete-ledger's completeness sweep still fits an office-hours
      sitting
    - strategy-attention-surface holds — the goals page is the rendering surface
      for mount exploration; no new app
    - mount structure never assumes same-repo residence — by-reference mounts
      (the private root inversion, household members, fork practitioners) must
      be the same shape
    - person-mounts are role-named and carry household consent where the person
      is identifiable, until the private-root migration lands
---
# Mounting is first-class — external graphs (delegatees, traditions, institutions, persons) graft on recursively as explorable structure with auditable degree of capture and deference
