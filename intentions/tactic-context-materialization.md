---
id: tactic-context-materialization
kind: tactic
statement: Materialize AI judgment context from the graph into the harness
  channels — tiered rules index, worktree CLAUDE.local.md projection,
  target-size compaction with a session-time compaction signal
owner: ai
status: raw
parent: null
rationale: "Delegated by the 2026-08-31 /align doctrine-alignment round under
  the ratified materialized-context principle (strategy-explicit-intent,
  2026-08-31), with author additions: the worktree CLAUDE.local.md must follow
  the same tier structure; the existing ancestry-context.md strategy is
  re-evaluated for function and token efficiency, not adopted wholesale;
  materialization compacts to a target context size, harness-memory style, with
  the author receiving a session-time compaction-level signal (a
  capture-monitoring hook — heavy compaction is a visible judgment-quality
  warning)."
reading: null
serves:
  - strategy-explicit-intent
  - strategy-token-economy
recovers: []
clarifications: []
tooling_goals: []
success_signal: null
attention: null
phase: null
execution: null
validates: []
blocked_by: []
superseded_by: []
supersession_expiry: null
office_hours: null
pace_exempt: false
rounds: null
attributes: {}
---
# Materialize AI judgment context from the graph into the harness channels — tiered rules index, worktree CLAUDE.local.md projection, target-size compaction with a session-time compaction signal

## Retained interview context (2026-08-31 /align doctrine-alignment round)

- Ordering for compaction comes from the rank triage charter
  (strategy-graph-drives-dispatch, 2026-08-31): backbone importance composed
  with task relevance; the tier list is DERIVED, never a stored classification
  (the stored-list staleness disease is what the projection principle exists
  to avoid).
- A minimal ratified BINDING FLOOR always loads in full — content that is
  mechanically load-bearing regardless of relevance (e.g. sandbox mechanics).
  The floor principle is ratified; the floor's membership classification is
  DEFERRED to author review when drafted — enrolled sitting:
  tactic-review-binding-floor-classification.
- Scope includes bringing .claude/rules/ and harness-memory governance under
  graph materialization so the audit apparatus covers what the AI actually
  reads. Measured 2026-08-31: .claude/rules/measurement-and-provenance.md
  binds every session with zero graph anchoring.
- Mitigates the tunnel-vision failure mode this round was designed around: AI
  judgment without necessary graph context becomes structural rather than
  discipline-dependent — every session in a worktree gets the claimed node's
  related tactics, strategies, and principles without remembering to load
  them.
