---
id: tactic-workflow-entry-point
kind: tactic
statement: Publish a standing practitioner entry point for the dispatch workflow
  — the chain as a thing a practitioner can evaluate and fork without adopting
  the whole repo
owner: human
status: raw
parent: null
rationale: "The entry-point-only half of the original node: its
  architecture-writeup half was split out to
  tactic-workflow-architecture-writeup on 2026-07-11 per the strategy's
  2026-07-06 clarification (the writeup is ungated voice; the standing entry
  point is an invitation). Remains a deliberate draft — gated on the tier-3
  entry declaration (tactic-tier3-entry-declaration) with the support boundary
  written first — and is the prerequisite for the tier-3 fork signals
  strategy-owned-orchestration's built-to-be-left claim waits on. Meeting the
  strategy threshold's entry-point half makes this a validates-terminal when it
  eventually lands."
reading: null
gap: null
serves:
  - strategy-distribute-workflow
recovers: []
clarifications: []
tooling_goals: []
success_signal: null
attention: null
phase: null
execution: null
validates:
  - strategy-distribute-workflow
blocked_by:
  - tactic-tier3-entry-declaration
  - tactic-practitioner-support-boundary
office_hours: null
pace_exempt: false
rounds: null
attributes: {}
---
# Publish a standing practitioner entry point for the dispatch workflow — the chain as a thing a practitioner can evaluate and fork without adopting the whole repo

Deliberate draft (no phase), rewritten to the entry-point-only half by the
2026-07-11 /align-tactics round: the architecture-writeup half now lives at
tactic-workflow-architecture-writeup. The standing entry point is an
invitation, so it waits on the tier-3 entry declaration
(tactic-tier3-entry-declaration) with the support boundary
(tactic-practitioner-support-boundary) written first — both recorded as
blocked_by edges. Its shape is design-dependent (what "the chain, forkable
without adopting the whole repo" is depends on the plugin/packaging design
still in flux), so a later /align-tactics round finalizes and plans it once
the gate clears. When it lands it meets the entry-point half of the
strategy's threshold — the validates edge records that.
