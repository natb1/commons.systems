## 1

Asked, on a later date, whether a second file's entries start over at 1.

> Each date's ordinals start at 1; readWords keys entries by address across
> every file, so two files may each hold a '## 1'.
