{
  "name": "@commons.systems/disposition",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Reader, validator, and browser projector for the disposition graph",
  "scripts": { "test": "node --test *.test.mjs" },
  "dependencies": { "yaml": "^2" }
}
