// Stub Hero (Unit 5 fills verbatim chips/FAQ). Capital-H filename avoids a
// .ts/.tsx collision with the existing imperative pages/hero.ts (renderHero),
// which is left on disk until later units retire it.
export function Hero() {
  return <section className="hero" />;
}
